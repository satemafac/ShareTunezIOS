"""Tests for the `music` app.

Replaces the single-file `tests.py` stub (which had no assertions). Django's
test runner discovers `test*.py` in here; `test_parsers.py` additionally runs
standalone under plain unittest, with no settings module required.
"""
